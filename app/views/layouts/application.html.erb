<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Attakke?" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag 'favicon.svg' %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="relative pt-18 md:pt-24 min-h-screen bg-dull-beige text-base text-f-body">
    <%= render 'shared/confirm' %>
    <%= render 'shared/flash_message' %>
    <%= render 'shared/header' %>
    <main class="container mx-auto pb-16">
      <%= yield %>
    </main>
    <%= render 'shared/footer' %>
    <%= turbo_frame_tag 'modal_frame' %>
  </body>
</html>
