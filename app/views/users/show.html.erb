<%= render 'shared/modal_wrapper' do %>

  <h2>ユーザー情報</h2>

  <p><strong>名前:</strong> <%= current_user.name %></p>
  <p><strong>メールアドレス:</strong> <%= current_user.email %></p>

  <%= link_to "編集する", edit_user_registration_path, class: "btn" %>
<% end %>
