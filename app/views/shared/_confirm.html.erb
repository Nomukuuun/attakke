<%# 背景 %>
<div
  data-controller="confirm"
  data-confirm-target="modal"
  data-action="click->confirm#clickOutside"
  class="fixed inset-0 bg-black/50 flex items-center justify-center hidden z-50"
>

  <%# 確認メッセージ %>
  <div data-confirm-target="dialog" class="bg-white p-4 rounded shadow">
    <p data-confirm-target="message" class="mb-4 text-lg"></p>

    <%# ボタン系 <= confirmボタンはデフォルトでは「削除」表示 %>
    <div class="flex justify-end space-x-4">
      <button
        type="button"
        data-confirm-target="confirmButton"
        data-action="click->confirm#confirm"
        class="px-4 py-2 bg-dull-red rounded hover:brightness-70"
      >
      </button>
      <button
        type="button"
        data-confirm-target="cancelButton"
        data-action="click->confirm#cancel"
        class="px-4 py-2 bg-gray-300 rounded hover:bg-gray-400"
      >
        キャンセル
      </button>
    </div>
  </div>
</div>
