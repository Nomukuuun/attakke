<% @stocks.each do |stock| %>
  <li class="flex items-center bg-white p-2 rounded hover:brightness-70" role="option" data-autocomplete-value="<%= stock.id %>" data-autocomplete-label="<%= stock.name %>">
    <%= stock.name %>
  </li>
<% end %>
