<%= turbo_frame_tag stock do %>
  <div class="bg-dull-beige p-4 rounded border border-box border-black mb-2 lg:mb-0">

    <div class="flex justify-between items-center mb-4">
      <p class="text-f-head text-xl font-bold"><%= stock.name %></p>
      <%= render 'stocks/quantity_icon', stock: stock %>
    </div>

    <div class="flex justify-between items-center">
      <div class="text-gray-600">最終更新日：<%= stock.number_of_days_elapsed %></div>
      <div class="flex justify-end items-center space-x-2">
        <%= link_to edit_stock_path(stock), data: { turbo_frame: 'modal_frame' } do %>
          <svg class="size-5 text-f-head" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M17 3a2.828 2.828 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z" />
          </svg>
        <% end %>
        <%= link_to stock_path(stock), data: { turbo_method: :delete, turbo_confirm: t('defaults.confirm.stock'), turbo_frame: stock } do %>
          <svg class="size-5 text-f-body" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
            <path stroke="none" d="M0 0h24v24H0z"/><line x1="4" y1="7" x2="20" y2="7" /><line x1="10" y1="11" x2="10" y2="17" /><line x1="14" y1="11" x2="14" y2="17" />
            <path d="M5 7l1 12a2 2 0 0 0 2 2h8a2 2 0 0 0 2 -2l1 -12" />
            <path d="M9 7v-3a1 1 0 0 1 1 -1h4a1 1 0 0 1 1 1v3" />
          </svg>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
